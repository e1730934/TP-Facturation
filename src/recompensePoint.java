
public abstract class recompensePoint implements recompense {

	/*
	 * @Override public void recompenser(FacturationAvecCarte f) throws Exception {
	 * if(!(f.card instanceof cartePoint)) { throw new
	 * Exception("La carte client n'est pas Airmiles"); } }
	 */
}
